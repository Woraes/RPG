services:
  rpg-game:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rpg-game
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
    restart: unless-stopped
